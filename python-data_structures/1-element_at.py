#!/usr/bin/python3
def element_at(my_list, idx):
    for idx in my_list:
        if idx == negative:
            print("None")
            if idx > number of elements in my_list:
                print("None")
